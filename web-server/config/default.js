module.exports =  {
  
  domain: 'localhost',

  port: '8080',

  // set to the current directory.
  baseDir: process.cwd(), 
  
  root: '/index.html',

  // no debug.
  isDebug: false,

  // no endpoint by default.
  endPointsFilePath: null,

  // no delay by default.
  delay: 0,
  
  withCORS: false
};