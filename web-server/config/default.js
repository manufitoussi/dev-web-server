module.exports = {

  domain: 'localhost',

  port: '8080',

  // set to the current directory.
  baseDir: process.cwd(),

  root: '/index.html',

  // no debug.
  isDebug: false,

  // no endpoint by default.
  endPointsFilePath: null,

  // endpoint root url.
  endPointsRoot: '/api',

  // no SPA by default.
  isSPA: false,

  // no delay by default.
  delay: 0,

  // no CORS by default.
  withCORS: false,

  // no cache by default.
  withCache: false,
};
